import '../../config/brand_colors.dart';

/// Палитра редактора карты (брендбук).
abstract final class MapEditorTheme {
  static const canvas = BrandColors.canvas;
  static const cream = BrandColors.milk;
  static const needles = BrandColors.needles;
  static const clay = BrandColors.clay;
  static const divider = BrandColors.borderSubtle;
  static const text = BrandColors.tar;
  static const onScene = BrandColors.onNeedles;
}
